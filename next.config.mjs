/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
       domains:[
        "cdn.pixabay.com",
        "pixabay.com",
        "lh3.googleusercontent.com"
       ]
      },
};

export default nextConfig;
